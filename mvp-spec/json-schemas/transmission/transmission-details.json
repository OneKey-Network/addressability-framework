{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Details",
    "description": "The details of the status. It can be empty for \"success\" but it should details the reason(s) in case of an error.",
    "$id": "details",
    "type": "string",
    "examples": [
        "No signature in the Transaction Request."
    ]
}